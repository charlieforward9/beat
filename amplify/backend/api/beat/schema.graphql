type Fuel @model @auth(rules: [{allow: public}]) {
  id: ID!
  percentage: Float
  goal: Float
}

type Network @model @auth(rules: [{allow: public}]) {
  id: ID!
  percentage: Float
  goal: Float
}

type Productivity @model @auth(rules: [{allow: public}]) {
  id: ID!
  percentage: Float
  goal: Float
}

type Fitness @model @auth(rules: [{allow: public}]) {
  id: ID!
  percentage: Float
  goal: Float
}

type Recovery @model @auth(rules: [{allow: public}]) {
  id: ID!
  percentage: Float
  goal: Float
}
 